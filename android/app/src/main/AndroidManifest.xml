<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spam.caller">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for CallScreeningService -->
    <uses-permission android:name="android.permission.BIND_SCREENING_SERVICE" />

    <application
        android:name=".SpamApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="SpamCaller"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpamCaller">

        <service
            android:name=".service.SpamCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BlockedListActivity" android:exported="false" />
    </application>

</manifest>
